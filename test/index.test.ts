import Code from '@hapi/code'
import Lab from '@hapi/lab'

const { describe, it } = exports.lab = Lab.script()
const { expect } = Code

describe('test', () => {
  it('test', async () => {
    expect(true)
  })
})
